{
  "name": "virtual-learning-management-system",
  "version": "1.0.0",
  "description": "This repository contains a full-stack Virtual Learning Management System application with a Node.js backend and a React + Vite frontend.",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend"
  },
  "author": "Ahmad Fadil Alfarisy",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
